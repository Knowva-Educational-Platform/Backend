export enum Role {
  STUDENT = 'STUDENT',
  TEACHER = 'TEACHER',
}

export enum genderType {
  male = 'MALE',
  female = 'FEMALE'
}

export enum matrialType {
  video = 'video',
  pdf = 'pdf',
  document = 'document'

}